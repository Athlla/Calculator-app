import styles from './Display.module.scss';

const Display = () => {
  return (
    <div className={styles.Display}>
      <span className={styles.Result}>123</span>
    </div>
  );
};

export default Display;
